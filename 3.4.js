// Program displaying the minimum and maximum of given array

let testArr = [1, 2, 10, 3, 4, 5];
let testArr2 = [100, 101, 102, 2285, 485];

let max = Math.max(...testArr2);
let min = Math.min(...testArr2);

console.log("This arrays/' minmum value is " + min + " and its maximum value is " + max + ".");